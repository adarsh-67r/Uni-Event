name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ── App: Format + Lint + Test ───────────────────────────────────────────────
  lint-and-test:
    name: Format, Lint & Test (app)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./app

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # Need full git access to commit formatted files back
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Auto-format with Prettier
        run: npx prettier --write .

      - name: Commit formatted files (push only)
        if: github.event_name == 'push'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git diff --quiet || (git add -A && git commit -m "style: auto-format with prettier [skip ci]" && git push)

      - name: Run ESLint
        run: npm run lint

      - name: Run Jest tests
        run: npm test -- --ci --passWithNoTests

  # ── Cloud Functions: Build ────────────────────────────────────────────────
  build-functions:
    name: Build (cloud-functions)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./cloud-functions

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Build TypeScript
        run: npm run build
